<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="@android:style/Theme.NoTitleBar"/>

    <style name="Theme.DarkerWallpaperSettings" parent="@android:style/Theme.Translucent">
        <item name="android:windowBackground">@color/partially_translucent_black</item>
    </style>

    <drawable name="translucent_background">#99000000</drawable>

    <style name="Theme.GrayTranslucent" parent="@android:style/Theme.Translucent">
        <item name="android:windowBackground">@drawable/translucent_background</item>
    </style>

    <style name="Theme.Transparent" parent="android:Theme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@drawable/transparent_background</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="MyRating" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/rating</item>
        <item name="android:minHeight">@dimen/adjusted_size_32dp</item>
        <item name="android:maxHeight">@dimen/adjusted_size_32dp</item>
        <item name="android:thumb">@null</item>
        <item name="android:numStars">5</item>
        <item name="android:stepSize">1</item>
        <item name="android:rating">2</item>
        <item name="android:isIndicator">false</item>
    </style>

</resources>